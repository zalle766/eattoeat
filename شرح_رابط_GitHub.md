# شرح رابط GitHub

## الرابط:
```
https://github.com/zalle766/eattoeat/blob/main/frontend
```

---

## معنى كل جزء:

### 1. `github.com`
- موقع GitHub (منصة تخزين الكود)

### 2. `zalle766`
- اسم المستخدم (حسابك على GitHub)

### 3. `eattoeat`
- اسم المستودع (Repository) - مشروعك

### 4. `blob`
- **Blob** = Binary Large Object
- يعني أنك تتصفح **ملف أو مجلد** في المستودع
- مقابل `tree` (عرض الملفات) أو `commit` (عرض التغييرات)

### 5. `main`
- اسم الفرع (Branch) - الفرع الرئيسي للمشروع
- كان يسمى `master` سابقاً، الآن `main` هو الافتراضي

### 6. `frontend`
- المجلد/الملف الذي تتصفحه

---

## ماذا يعني هذا الرابط؟

✅ **يعني أن:**
- مجلد `frontend` **موجود** في المستودع على GitHub
- يمكن الوصول إليه عبر هذا الرابط
- الملفات داخل `frontend` يجب أن تكون مرئية

---

## إذا كان المجلد فارغاً:

إذا دخلت على الرابط ووجدت أن `frontend` فارغ أو لا تظهر الملفات:

### السبب:
- الملفات داخل `frontend` غير متتبعة في Git
- تم رفع المجلد فقط بدون محتوياته

### الحل:
```bash
# إضافة جميع ملفات frontend
git add frontend/

# عمل commit
git commit -m "Add frontend files"

# رفع إلى GitHub
git push
```

---

## أنواع الروابط في GitHub:

### 1. `blob` - ملف أو مجلد:
```
https://github.com/user/repo/blob/main/folder
```

### 2. `tree` - عرض الملفات:
```
https://github.com/user/repo/tree/main/folder
```

### 3. `commit` - عرض التغييرات:
```
https://github.com/user/repo/commit/abc123
```

---

## الخلاصة:

الرابط `https://github.com/zalle766/eattoeat/blob/main/frontend` يعني:
- ✅ مجلد `frontend` موجود في المستودع
- ✅ على الفرع `main`
- ✅ يمكن الوصول إليه عبر GitHub

إذا كانت الملفات غير مرئية، استخدم السكريبتات التي أنشأناها لرفع الملفات! 🚀

