# 🚀 تعليمات تشغيل Eat to Eat

## المشكلة الحالية:
الصفحة تظهر فارغة في `localhost:3000` - هذا يعني أن هناك مشكلة في React أو الخادم.

## الحلول المتاحة:

### 1. اختبار بسيط (موصى به أولاً):
1. انقر نقراً مزدوجاً على `start_simple_server.bat`
2. انتظر حتى يبدأ الخادم
3. افتح المتصفح واذهب إلى: `http://localhost:3000/test.html`
4. يجب أن ترى صفحة جميلة مع رسالة ترحيب

### 2. تشغيل النسخة الكاملة مع React:
1. تأكد من أن Node.js مثبت على جهازك
2. انقر نقراً مزدوجاً على `start_dev_server.bat`
3. انتظر حتى يبدأ الخادم
4. افتح المتصفح واذهب إلى: `http://localhost:3000`

### 3. تشغيل يدوي:
1. افتح Command Prompt (ليس PowerShell)
2. اكتب: `cd C:\xampp\htdocs\eattoeat\frontend`
3. اكتب: `npm run dev`
4. انتظر حتى يبدأ الخادم

## استكشاف الأخطاء:

### إذا لم يعمل الخادم البسيط:
- تأكد من أن Python مثبت
- جرب فتح `test.html` مباشرة في المتصفح

### إذا لم يعمل React:
- تأكد من أن Node.js مثبت
- تأكد من أن npm يعمل
- جرب حذف مجلد `node_modules` وإعادة التثبيت

## الملفات المتاحة:
- `test.html` - صفحة اختبار بسيطة
- `start_simple_server.bat` - خادم بسيط
- `start_dev_server.bat` - خادم React
- `main_test.jsx` - React بسيط
- `main_simple.jsx` - React كامل

## الميزات المتاحة:
- ✅ مودال تحديد الموقع
- ✅ عرض المطاعم القريبة
- ✅ حساب المسافات
- ✅ بحث المطاعم
- ✅ تصميم متجاوب وجميل

جرب الخادم البسيط أولاً وأخبرني بالنتيجة! 🎯
